[package]
name = "spreadless-interface-tests"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
spreadless-pool-interface = { path = "../spreadless-pool-interface" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
